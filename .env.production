# Production Environment Configuration
# 生产环境配置模板

# Application Environment
APP_ENV=production
LOG_LEVEL=INFO

# Database Configuration
# 从 anker-launch 平台控制台获取数据库连接信息
# Get database connection from anker-launch platform dashboard
DATABASE_URL=postgresql+asyncpg://username:password@hostname:5432/database_name

# Redis Configuration
# 如果平台提供 Redis，从控制台获取连接信息
# If platform provides Redis, get connection info from dashboard
# 或者使用内存模式（不推荐生产环境）: REDIS_URL=memory://
REDIS_URL=redis://hostname:6379/0

# JWT Configuration
# 重要：使用强密钥！使用以下命令生成：openssl rand -hex 32
# IMPORTANT: Use a strong secret key! Generate with: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_KEY
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# API Configuration
API_V1_PREFIX=/api/v1
# 允许的跨域来源，多个域名用逗号分隔
# Allowed CORS origins, separate multiple domains with commas
CORS_ORIGINS=https://your-domain.com,https://go.anker-launch.com

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Notification Service (Optional)
# FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json
# PUSH_NOTIFICATION_ENABLED=false
